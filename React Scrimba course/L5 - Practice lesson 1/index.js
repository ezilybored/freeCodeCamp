// Import the required libraries
import React from "react"
import ReactDOM from "react-dom"

// Renders to the DOM an unordered list with 3 elements
ReactDOM.render(<ul>
    <li>Whoa</li>
    <li>Yeah</li>
    <li>Cool, Yeah</li>
  </ul>,
  // The target for the html to be rendered
  document.getElementById("root")
)
